<nav class="flex justify-between bg-teal-800 text-white px-24 py-3">
    <div>
        <h1 class="font-black"><%= link_to "BookS", root_path %></h1>
    </div>
    <ul class="flex gap-3">
        <% if user_signed_in? %>
            <li><%= current_user.email %></li>
            <li><%= link_to "登出", sessions_path, data: { turbo_method: "delete" } %></li>
        <% else %>
            <li><%= link_to "註冊", new_users_path %></li>
            <li><%= link_to "登入", sign_in_users_path %></li>
        <% end %>
    </ul>
</nav>